export namespace COMPONENTS {
    let TRACKING_TARGET: string;
    let TRANSFORM: string;
    let VISIBLE: string;
}
export namespace RESOURCES {
    let PROCESSING_CONFIG: string;
    let CAPTURE_STATE: string;
    let FRAME_SOURCE_REF: string;
    let DEVICE_PROFILE: string;
    let ENABLED_PLUGINS: string;
}
export namespace EVENTS {
    let CAPTURE_INIT_START: string;
    let CAPTURE_INIT_SUCCESS: string;
    let CAPTURE_INIT_ERROR: string;
    let CAPTURE_READY: string;
    let CAPTURE_DISPOSED: string;
    let SOURCE_LOADED: string;
    let SOURCE_ERROR: string;
    let SOURCE_PLAYING: string;
    let SOURCE_PAUSED: string;
    let FRAME_PROCESSED: string;
    let ENGINE_START: string;
    let ENGINE_STOP: string;
    let ENGINE_UPDATE: string;
    let PLUGIN_REGISTERED: string;
    let PLUGIN_ENABLED: string;
    let PLUGIN_DISABLED: string;
}
export namespace CAPTURE_STATES {
    let UNINITIALIZED: string;
    let INITIALIZING: string;
    let READY: string;
    let ERROR: string;
    let DISPOSED: string;
}
export namespace SOURCE_TYPES {
    let WEBCAM: string;
    let VIDEO: string;
    let IMAGE: string;
}
export namespace DEVICE_PROFILES {
    let DESKTOP_FAST: string;
    let DESKTOP_NORMAL: string;
    let PHONE_NORMAL: string;
    let PHONE_SLOW: string;
}
export namespace QUALITY_TIERS {
    let LOW: string;
    let MEDIUM: string;
    let HIGH: string;
    let ULTRA: string;
}
//# sourceMappingURL=components.d.ts.map